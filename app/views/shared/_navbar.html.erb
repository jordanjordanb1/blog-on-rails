<nav class="bg-blue-600 shadow-lg">
  <div class="max-w-6xl mx-auto px-2 sm:px-6 lg:px-8">
    <div class="relative flex items-center justify-between h-16">
      <div class="flex-1 flex items-center justify-center sm:items-stretch sm:justify-start">
        <div class="flex-shrink-0 flex items-center">
          <%= link_to "BLOG", root_path, class: "text-white text-2xl" %>
        </div>
        <div class="block w-full sm:ml-6">
          <div class="flex space-x-4 w-full justify-between">
            <div class="flex">
              <%= link_to "HOME",
                        root_path,
                        class: "
                          text-white hover:bg-gray-700 hover:bg-opacity-10 px-3 py-2 rounded-md text-sm font-medium"
              %>
            </div>

            <div class="flex">
              <% if user_signed_in? %>
                <%= link_to "LOG OUT",
                            destroy_user_session_path,
                            method: :delete,
                            class: "
                            text-white hover:bg-gray-700 hover:bg-opacity-10 px-3 py-2 rounded-md text-sm font-medium"
                %>
              <% else %>
                <%= link_to "REGISTER",
                            new_user_registration_path,
                            class: "
                            text-white hover:bg-gray-700 hover:bg-opacity-10 px-3 py-2 rounded-md text-sm font-medium"
                %>

                <%= link_to "LOG IN",
                            new_user_session_path,
                            class: "
                            text-white hover:bg-gray-700 hover:bg-opacity-10 px-3 py-2 rounded-md text-sm font-medium"
                %>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</nav>
